# SMTP (required for HR emails to send)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=true
EMAIL_FROM=DocPass <noreply@docpass.co.uk>

# Already set on Oracle
BASE_URL=https://docpass.co.uk

# Optional pilot: route all HR digest/instant emails here (login email unchanged)
# HR_EMAIL_OVERRIDE=you@nhs.net
