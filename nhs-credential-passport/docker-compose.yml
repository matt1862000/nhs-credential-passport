# Local or single-server run with Docker
# Usage: docker compose up --build
# Keys and DB live in the container (persist until you remove it). For production, use Render/Fly.
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - BASE_URL=http://localhost:8000
      - PORT=8000
